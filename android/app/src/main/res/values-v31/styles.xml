<?xml version="1.0" encoding="utf-8"?>
<!-- android/app/src/main/res/values-v31/styles.xml -->
<!-- Android 12+ Splash Screen API — eliminates icon flash -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar.Fullscreen">
        <!-- Pure white splash — no icon -->
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <!-- Transparent 1x1 drawable prevents system from showing adaptive icon -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_blank</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>
</resources>
